class TransactionCategoryDistribution {
  String label;
  double percentage;
  double amount;

  TransactionCategoryDistribution({
    required this.label,
    required this.percentage,
    required this.amount,
  });
}

class TransactionSourceDistribution {
  String label;
  double percentage;
  double amount;

  TransactionSourceDistribution({
    required this.label,
    required this.percentage,
    required this.amount,
  });
}
